#include <stdio.h>

/**
 * main - prints numbers between 1 and 50
 *
 * Return: 0 if successful
 */

int main(void)
{
	int i = 0;

	i = i + 1;
	while (i <= 49)
	{
		printf("%d ", i);
		i = i + 1;
	}
	printf("\n");
	return (0);
}
